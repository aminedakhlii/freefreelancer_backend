FLASK_ENV=development
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key
OPENAI_API_KEY=your-openai-key
# From Supabase: Project Settings → API → JWT Settings → JWT Secret (optional; if unset, tokens are verified via Supabase Auth API)
JWT_SECRET=your-jwt-secret-from-supabase
# Comma-separated allowed origins (default: http://localhost:4200)
# CORS_ORIGINS=http://localhost:4200,https://your-domain.com
